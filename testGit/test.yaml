name: Test Project

on: 
  pull_request:
    branches:
      - main

jobs: 
  test: 
    runs-on: ubuntu-latest
    container: 
     image: node:20
    - run: npm ci
    - run: npm run build
    - name: Get SSH key and get permissions
      run: |
        mkdir -p ~/.ssh
        echo "ssh key stuff here"
    - name: Deploy 